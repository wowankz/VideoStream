{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "outDir": "../out-tsc/app",
        "types": [
            "chrome",
            "node"
        ]
    },
    "exclude": [
        "test.ts",
        "**/*.spec.ts"
    ]
}